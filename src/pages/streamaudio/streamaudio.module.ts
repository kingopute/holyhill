import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { StreamaudioPage } from './streamaudio';

@NgModule({
  declarations: [
   // StreamaudioPage,
  ],
  imports: [
    IonicPageModule.forChild(StreamaudioPage),
  ],
})
export class StreamaudioPageModule {}
